export type Bindings = {
    // vacío por ahora — no hay bindings reales todavía en esta plantilla.
    // Se va completando a medida que agregues D1, ASSETS, etc.
};
export type AppEnv = { Bindings: Bindings };